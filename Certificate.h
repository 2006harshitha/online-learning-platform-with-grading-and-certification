using namespace std;
class Certificate{
	public:
		int cal_of_tot_marks(string u_name,int c);
		void generateCertificate(string userName, int c, int score);
		void displaycertificates(string );
};
